# V2 M1 Art Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the V2 M1 graybox into a visually readable first-chapter slice without changing V1.

**Architecture:** Keep `ArtCatalog` as the single runtime lookup layer. Improve the V2 battle presentation in `battle_controller.gd` and `v2_battle_controller.gd` without changing mission rules. Add or revise only V2 runtime assets and register every asset in the existing manifest.

**Tech Stack:** Godot 4.7.1, GDScript, PNG with transparent backgrounds, existing V2 `ArtCatalog`, PowerShell test gate, generated visual snapshot matrix.

## Global Constraints

- V2 resources live under `tactical-grid/client/assets/generated/chapter1/runtime`.
- V2 runtime must not read V1 worktree paths or V1 save data.
- Keep the existing Echo Yard blue/orange industrial style.
- Use existing legal/generated assets first; new generated assets require manifest entries.
- Every visual change must be verified at both 1280x720 and 1920x1080.

---

### Task 1: Establish the M1 art baseline

**Files:**
- Create: `tactical-grid/client/tests/v2/v2_m1_art_baseline_test.gd`
- Modify: `tactical-grid/client/tests/v2/v2_player_turn_e2e_test.gd`
- Modify: `tactical-grid/client/data/v2/resource_manifest.md`

**Interfaces:**
- Consumes: `ArtCatalog.has_texture(kind, key)`, the V2 M1 battle scene, and existing screenshot helpers.
- Produces: deterministic assertions for the four M1 units, three objectives, Echo Yard environment kit, and V2-only asset paths.

- [x] **Step 1: Write failing baseline assertions** for `unit/assault`, `unit/scout`, `unit/sentry_basic`, `unit/attack_drone`, `network_node/camera`, `objective/evac`, and the rescue objective texture.
- [x] **Step 2: Run the focused test** and record missing keys before changing assets.
- [x] **Step 3: Add the manifest entries** for every already-present runtime asset, including dimensions and “AI generated / project-owned” provenance.
- [x] **Step 4: Run the focused test again** and require zero missing M1 visual dependencies.
- [x] **Step 5: Commit** covered by the grouped M1 art integration commit.

### Task 2: Integrate readable M1 unit and objective art

**Files:**
- Modify: `tactical-grid/client/scripts/data/art_catalog.gd`
- Modify: `tactical-grid/client/scripts/game/unit_sprite.gd`
- Modify: `tactical-grid/client/scripts/game/battle_controller.gd`
- Modify: `tactical-grid/client/assets/v2/README.md`
- Create or modify: `tactical-grid/client/assets/generated/chapter1/runtime/units/*`
- Create or modify: `tactical-grid/client/assets/generated/chapter1/runtime/objectives/*`

**Interfaces:**
- Consumes: `ArtCatalog.get_texture`, `UnitSprite.update_unit`, `_create_unit_sprite`, and the existing objective/network node renderers.
- Produces: distinct V2 visual identities with no gameplay/stat changes.

- [x] **Step 1: Capture the current M1 start and selected screenshots** through the visual matrix to preserve a before reference.
- [x] **Step 2: Prepare the smallest required art set** using existing generated assets first; generate only the missing rescue beacon transparent PNG.
- [x] **Step 3: Preserve deterministic role tint and accent rules** for the existing V2 unit identities.
- [x] **Step 4: Register the camera, rescue, and evacuation textures** in `ArtCatalog` and render them above the tile layer with existing marker feedback.
- [x] **Step 5: Run the focused art, interaction, and player-turn tests; fix missing or invalid paths.**
- [x] **Step 6: Commit** covered by the grouped M1 art integration commit.

### Task 3: Improve Echo Yard composition and environment layering

**Files:**
- Modify: `tactical-grid/client/scripts/game/battle_controller.gd`
- Modify: `tactical-grid/client/scripts/v2/runtime/v2_battle_controller.gd`
- Modify: `tactical-grid/client/scripts/game/tactical_tile.gd`
- Modify: `tactical-grid/client/tests/v2/v2_player_turn_e2e_test.gd`
- Modify: `tactical-grid/client/data/v2/resource_manifest.md`
- Modify or create: `tactical-grid/client/assets/generated/chapter1/runtime/environment/echo_yard/*`

**Interfaces:**
- Consumes: existing Echo Yard environment kit, map dimensions, `_get_cell_center`, map camera node, and `_render_environment_decorations`.
- Produces: a centered, layered M1 view with repeatable terrain variation and no gameplay collision changes.

- [x] **Step 1: Verify composition evidence** through the visual matrix at 1280x720 and 1920x1080.
- [x] **Step 2: Preserve the V2 initial camera framing** and HUD-safe area without modifying map coordinates or V1 camera code.
- [x] **Step 3: Use the existing Echo Yard floor, edge, prop, and landmark variants** with deterministic map decorations.
- [x] **Step 4: Add high-contrast landmarks near route branches and keep collision data unchanged.**
- [x] **Step 5: Run the visual matrix** for start, selected, attack preview, rescue, evac, dialogue, and result stages in both resolutions.
- [x] **Step 6: Commit** with the grouped M1 art integration commit.

### Task 4: Visual polish and release verification

**Files:**
- Modify: `tactical-grid/client/scripts/game/battle_controller.gd`
- Modify: `tactical-grid/client/tests/v2/v2_player_turn_e2e_test.gd`
- Modify: `tactical-grid/client/data/v2/resource_manifest.md`
- Modify: `tactical-grid/client/assets/v2/README.md`

**Interfaces:**
- Consumes: the integrated unit/objective/environment art and current attack/rescue/evac feedback hooks.
- Produces: release evidence and a clean V2 asset audit.

- [x] **Step 1: Assert** that movement, attack, rescue, and evacuation feedback nodes are present and visible in their corresponding E2E states.
- [x] **Step 2: Run `git diff --check` and the focused V2 visual tests.**
- [x] **Step 3: Run `powershell -ExecutionPolicy Bypass -File tests/v2/run_v2_gate.ps1` and require `V2 RELEASE GATE PASSED`.**
- [x] **Step 4: Review all generated screenshots for clipping, black-bar dominance, sprite overlap, unreadable colors, and HUD occlusion; correct only verified defects.**
- [x] **Step 5: Commit** with the grouped M1 art integration commit.
